name: Flutter Automate workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: cirrusci/flutter

    steps:
    - uses: actions/checkout@master
      with:
        fetch-depth: 1
#    - uses: actions/setup-java@v1
#      with:
#        java-version: '12.x'
    - name: Check all thing done
      run: flutter doctor
    - name: Install dependencies
      run: flutter pub get
#  test:
#    needs: [build]
#    runs-on: ubuntu-latest
#    steps:
#    - name: Run All Tests
#      run: flutter test
